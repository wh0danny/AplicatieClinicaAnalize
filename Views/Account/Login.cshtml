@using AplicatieClinicaAnalize.Data.ViewModels;

@model LoginVM;

@{
    ViewData["Title"] = "Intrati in account";
}

<div class="row">
    <div class="col-md-6 offset-3">
        <p>
            <h4>Intrati in accountul dvs.</h4>
        </p>

        <div class="row">
            <div class="col-md-8 offset-2">
                <form asp-action="Login">
                    <div class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="EmailAddress" class="control-label"></label>
                        <input asp-for="EmailAddress" class="form-control"/>
                    </div>
                    <div class="form-group">
                        <label asp-for="Password" class="control-label"></label>
                        <input asp-for="Password" class="form-control"/>
                    </div>

                    <div class="form-group">
                        <input class="btn btn-outline-success float-end" type="submit" value="Log In" />
                        <a class="btn btn-outline-secondary" asp-controller="Analize" asp-action="Index">Cancel</a>
                    </div>

                </form>
            </div>
        </div>

    </div>
</div>